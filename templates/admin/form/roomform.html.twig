
{% block form %}
<div class="col-6">
<div class="form col-10">
    {{ form_start(roomForm, {'attr': {'novalidate': 'novalidate'}}) }}
    {{ form_widget(roomForm) }}
    
    <button type="submit" class="btn btn-primary">Save</button>
    {{ form_end(roomForm) }}
    </div>
    </div>
{% endblock %}