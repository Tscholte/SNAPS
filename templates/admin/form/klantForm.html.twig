
{% block form %}
<div class="col-6">
<div class="form col-10">
    {{ form_start(klantForm, {'attr': {'novalidate': 'novalidate'}}) }}
    {{ form_widget(klantForm) }}
    
    <button type="submit" class="btn btn-primary">Save</button>
    {{ form_end(klantForm) }}
    </div>
    </div>
{% endblock %}